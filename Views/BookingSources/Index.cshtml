@model IEnumerable<itehad.Models.BookingSource>
@using itehad.Helpers
@{
    ViewBag.Title = "مصادر الحجز";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div></div>
    <button type="button" class="btn btn-accent" data-bs-toggle="modal" data-bs-target="#addSourceModal">@Icons.PlusCircle() مصدر جديد</button>
</div>

<div class="table-card">
    @if (Model.Any())
    {
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead>
                    <tr>
                        <th>اسم المصدر</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var s in Model)
                    {
                        <tr>
                            <td class="fw-semibold">@s.Name</td>
                            <td class="text-start d-flex gap-1 justify-content-end">
                                <button type="button" class="btn btn-sm btn-outline-secondary" data-bs-toggle="modal" data-bs-target="#editSourceModal_@s.Id">@Icons.Edit()</button>
                                @using (Html.BeginForm("Delete", "BookingSources", FormMethod.Post, new { style = "display:inline", data_confirm = "هل تريد حذف هذا المصدر؟" }))
                                {
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="id" value="@s.Id" />
                                    <button type="submit" class="btn btn-sm btn-outline-danger">@Icons.XCircle()</button>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="empty-state">
            @Icons.Tag()
            <div>لا توجد مصادر حجز مسجّلة بعد</div>
        </div>
    }
</div>

@foreach (var s in Model)
{
    <div class="modal fade" id="editSourceModal_@s.Id" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                @using (Html.BeginForm("Edit", "BookingSources", FormMethod.Post))
                {
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="Id" value="@s.Id" />
                    <div class="modal-header">
                        <h5 class="modal-title">تعديل مصدر الحجز</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
                    </div>
                    <div class="modal-body">
                        <label class="form-label">اسم المصدر</label>
                        <input type="text" name="Name" class="form-control" value="@s.Name" />
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">إلغاء</button>
                        <button type="submit" class="btn btn-accent">حفظ</button>
                    </div>
                }
            </div>
        </div>
    </div>
}

<div class="modal fade" id="addSourceModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            @using (Html.BeginForm("Create", "BookingSources", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                <div class="modal-header">
                    <h5 class="modal-title">مصدر حجز جديد</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
                </div>
                <div class="modal-body">
                    <label class="form-label">اسم المصدر</label>
                    <input type="text" name="Name" class="form-control" />
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">إلغاء</button>
                    <button type="submit" class="btn btn-accent">إضافة</button>
                </div>
            }
        </div>
    </div>
</div>
