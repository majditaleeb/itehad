@model IEnumerable<itehad.Models.Location>
@using itehad.Helpers
@{
    ViewBag.Title = "المواقع";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div></div>
    <button type="button" class="btn btn-accent" data-bs-toggle="modal" data-bs-target="#addLocationModal">@Icons.PlusCircle() موقع جديد</button>
</div>

<div class="table-card">
    @if (Model.Any())
    {
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead>
                    <tr>
                        <th>اسم الموقع</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var l in Model)
                    {
                        <tr>
                            <td class="fw-semibold">@l.Name</td>
                            <td class="text-start d-flex gap-1 justify-content-end">
                                <button type="button" class="btn btn-sm btn-outline-secondary" data-bs-toggle="modal" data-bs-target="#editLocationModal_@l.Id">@Icons.Edit()</button>
                                @using (Html.BeginForm("Delete", "Locations", FormMethod.Post, new { style = "display:inline", data_confirm = "هل تريد حذف هذا الموقع؟" }))
                                {
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="id" value="@l.Id" />
                                    <button type="submit" class="btn btn-sm btn-outline-danger">@Icons.XCircle()</button>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="empty-state">
            @Icons.MapPin()
            <div>لا توجد مواقع مسجّلة بعد</div>
        </div>
    }
</div>

@foreach (var l in Model)
{
    <div class="modal fade" id="editLocationModal_@l.Id" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                @using (Html.BeginForm("Edit", "Locations", FormMethod.Post))
                {
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="Id" value="@l.Id" />
                    <div class="modal-header">
                        <h5 class="modal-title">تعديل الموقع</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
                    </div>
                    <div class="modal-body">
                        <label class="form-label">اسم الموقع</label>
                        <input type="text" name="Name" class="form-control" value="@l.Name" />
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">إلغاء</button>
                        <button type="submit" class="btn btn-accent">حفظ</button>
                    </div>
                }
            </div>
        </div>
    </div>
}

<div class="modal fade" id="addLocationModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            @using (Html.BeginForm("Create", "Locations", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                <div class="modal-header">
                    <h5 class="modal-title">موقع جديد</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
                </div>
                <div class="modal-body">
                    <label class="form-label">اسم الموقع</label>
                    <input type="text" name="Name" class="form-control" />
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">إلغاء</button>
                    <button type="submit" class="btn btn-accent">إضافة</button>
                </div>
            }
        </div>
    </div>
</div>
